# CLAUDE.md

読書ノート用のナレッジリポジトリ。全体設計は `README.md` を参照。

## 言語

ノートは日本語で書く。ユーザーへの応答も日本語。

## 分担（最重要）

- **器（Claude）**: 書誌情報の調査、章立ての作成、テンプレート配置、索引更新、`topics/` への切り出し提案、表記の整理。
- **中身（ユーザー）**: 各章で何が重要だったか。要旨・解釈・引用はユーザーが書く。

Claudeは**空の器を正確に作るところまでが仕事**。章の中身をそれらしく埋めるのは、ユーザーの読書を上書きする行為なので絶対にやらない。口述された内容を整形・清書するのは歓迎。

## 本を追加するとき

1. `books/<書名>/` を作る。ディレクトリ名は日本語の書名（副題は落とす。例: `聖と俗　対話による宮台真司クロニクル` → `聖と俗`）。年やISBN、ローマ字化はしない。
2. `templates/` の3ファイルをコピーして配置する。
3. `index.md` のfrontmatterを埋める。書名・著者・出版社・発売日・ISBN・ページ数は**必ず出典（版元サイト、書店サイト等）で確認する**。推測で埋めない。
4. **目次をweb検索で取得し、`notes.md` に全章分の見出しを作る。** これはユーザーではなくClaudeの担当。
   - 版元サイトに目次がないことは多い。紀伊國屋書店（`kinokuniya.co.jp/f/dsg-01-<ISBN13>`）、版元ドットコム、honto、丸善ジュンク堂、e-honなどを当たる。逐語の目次が載っているのは書店系が多い。
   - 章題は**逐語で**入れる。英訳経由の要約や、AIが言い換えたものを章題にしない。
   - 逐語版がどうしても見つからないときだけ、見出しに「（仮）」を付け、出典と要確認であることをHTMLコメントで残す。
   - 小見出しまで取れた場合はHTMLコメントで併記しておく（実物と食い違う可能性があるため見出しには昇格させない）。
   - どの出典から起こしたかを `notes.md` 冒頭に1行書く。
5. `python3 scripts/build-index.py` で `books/README.md` を更新する。
6. ユーザーには「章立てを用意した。中身を埋めてほしい」と伝える。

## ノートを書くとき

- **本の内容を創作しない。** 読んでいない章の要旨を推測で埋めるのは最も避けるべき失敗。埋められない箇所は空欄のまま残す。
- 引用（`>`）は逐語。ユーザーが口述した内容を整形するのは可だが、本文引用として断定しない。
- ユーザーの解釈は `→`、疑問は `?`。この区別を勝手に混ぜない。
- ページ番号がわからないときは `p.?` と書く。それらしい数字を入れない。
- 同じ概念が2冊目に登場したら `topics/` への切り出しを提案する。

## コミット

- 1冊 or 1回の読書セッション = 1コミット。
- メッセージ例: `notes(seito-to-zoku): 第3章まで`, `topics: 亜社会 を追加`
